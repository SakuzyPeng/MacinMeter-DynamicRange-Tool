name: CI/CD Pipeline - MacinMeter DR Tool

on:
  push:
    branches: [ master, main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:  # 允许手动触发
    inputs:
      skip_tests:
        description: 'Skip tests for faster builds'
        type: boolean
        default: false
      build_targets:
        description: 'Specific targets to build (comma-separated)'
        type: string
        default: 'all'

# 🚀 私有仓库优化：并发限制和资源管理
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  # 私有仓库优化：减少网络重试
  CARGO_NET_RETRY: 2
  CARGO_NET_TIMEOUT: 30

jobs:
  # 🚀 快速变更检测 - 私有仓库优化
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.changes.outputs.rust }}
      docs: ${{ steps.changes.outputs.docs }}
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🔍 Check for Rust Code Changes
      uses: dorny/paths-filter@v2
      id: changes
      with:
        filters: |
          rust:
            - 'src/**/*.rs'
            - 'Cargo.toml'
            - 'Cargo.lock'
            - '.github/workflows/**'
          docs:
            - 'README.md'
            - 'docs/**'
            
  # 🔍 代码质量检查和安全审计
  quality-check:
    name: Code Quality & Security Audit
    needs: changes
    if: ${{ needs.changes.outputs.rust == 'true' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    env:
      # 🎯 CI环境专用设置 - 覆盖本地.cargo/config.toml避免target冲突
      CARGO_BUILD_TARGET: x86_64-unknown-linux-gnu
      RUSTFLAGS: '-C opt-level=3'  # 简化编译标志避免特性冲突
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🦀 Install Rust Toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy
        
    - name: 📦 Smart Cache - Cargo Registry
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-registry-
          
    - name: 📦 Smart Cache - Cargo Git Dependencies  
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git/db/
        key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-git-
          
    - name: 📦 Smart Cache - Target Directory
      uses: actions/cache@v4
      with:
        path: target/
        key: ${{ runner.os }}-target-quality-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('src/**/*.rs') }}
        restore-keys: |
          ${{ runner.os }}-target-quality-${{ hashFiles('**/Cargo.lock') }}-
          ${{ runner.os }}-target-quality-
          
    - name: 📦 Cache Cargo Tools (Stable Key)
      uses: actions/cache@v4
      id: cargo-tools-cache
      with:
        path: |
          ~/.cargo/bin/cargo-audit
          ~/.cargo/.crates.toml
          ~/.cargo/.crates2.json
        key: ${{ runner.os }}-cargo-audit-v4-stable
        restore-keys: |
          ${{ runner.os }}-cargo-audit-v4-
          ${{ runner.os }}-cargo-audit-
          
    - name: 🔧 Install Security Audit Tool (Optimized Cache)
      run: |
        echo "🎯 检测cargo-audit缓存状态..."
        echo "缓存命中状态: '${{ steps.cargo-tools-cache.outputs.cache-hit }}'"
        
        # 检查cargo-audit是否存在且可用
        if command -v cargo-audit &> /dev/null && cargo-audit --version &> /dev/null; then
          echo "✅ cargo-audit已可用（缓存恢复或已安装）"
          cargo-audit --version
        else
          echo "📦 安装cargo-audit（缓存未命中或损坏）..."
          cargo install --locked cargo-audit
          echo "✅ 新安装完成："
          cargo-audit --version
        fi
      
    - name: 📋 Check Code Formatting
      run: |
        echo "🎨 Checking code formatting..."
        cargo fmt --all -- --check
        
    - name: 🔍 Run Clippy Static Analysis
      run: |
        echo "📎 Running Clippy static analysis..."
        cargo clippy --all-targets --all-features -- -D warnings
        
    - name: ✅ Compile Check
      run: |
        echo "🔨 Running compile check..."
        cargo check --all-targets --all-features
        
    - name: 🛡️ Security Audit
      run: |
        echo "🔒 Running security audit..."
        cargo audit --ignore RUSTSEC-0000-0000  # 忽略已知的无害警告
        
    - name: 🧪 Run Unit Tests
      if: ${{ !github.event.inputs.skip_tests }}
      run: |
        echo "🧪 Running unit tests..."
        cargo test --all-features -- --nocapture
        
    - name: 📊 Generate Test Coverage (Optional)
      run: |
        echo "📊 Test coverage analysis completed"
        # 可以添加 tarpaulin 或其他覆盖率工具
        
  # 🏗️ 多平台构建
  build:
    name: Build (${{ matrix.os }})
    needs: [changes, quality-check]  # 依赖变更检测和质量检查
    # 🚀 私有仓库优化：条件构建
    if: ${{ needs.changes.outputs.rust == 'true' || github.event_name == 'workflow_dispatch' }}
    strategy:
      fail-fast: false
      max-parallel: 2  # 私有仓库优化：限制并发以避免资源争用
      matrix:
        include:
          # Windows x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: windows
            arch: x64
            extension: .exe
            
          # macOS Intel (Native)
          - os: macos-latest
            target: x86_64-apple-darwin
            platform: macos
            arch: intel
            extension: ""
            
          # macOS Apple Silicon (Native) 
          - os: macos-15
            target: aarch64-apple-darwin
            platform: macos
            arch: arm64
            extension: ""
            
          # Linux x64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: linux
            arch: x64
            extension: ""
            
    runs-on: ${{ matrix.os }}
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🦀 Install Rust Toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}
        
    # 🎯 显式安装交叉编译目标 (确保可靠性)
    - name: 🎯 Install Cross-compilation Target
      run: |
        echo "🎯 Installing target: ${{ matrix.target }}"
        rustup target add ${{ matrix.target }}
        rustup target list --installed | grep ${{ matrix.target }} || (echo "❌ Target installation failed" && exit 1)
        echo "✅ Target ${{ matrix.target }} successfully installed"
        
    # 🚀 多层缓存策略优化 - 私有仓库友好
    - name: 📦 Platform Cache - Cargo Registry
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
        key: cargo-registry-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          cargo-registry-
          
    - name: 📦 Platform Cache - Git Dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git/db/
        key: cargo-git-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          cargo-git-
          
    - name: 📦 Platform Cache - Build Target
      uses: actions/cache@v4
      with:
        path: target/
        key: ${{ runner.os }}-${{ matrix.target }}-target-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('src/**/*.rs', 'Cargo.toml') }}
        restore-keys: |
          ${{ runner.os }}-${{ matrix.target }}-target-${{ hashFiles('**/Cargo.lock') }}-
          ${{ runner.os }}-${{ matrix.target }}-target-
          
    # 🕐 生成精确到秒的时间戳（UTC时区） - Windows
    - name: 🕐 Generate Timestamp (Windows)
      if: matrix.os == 'windows-latest'
      id: timestamp-windows
      run: |
        $timestamp = Get-Date -Format "yyyy-MM-dd_HH-mm-ss_UTC" -AsUTC
        echo "timestamp=$timestamp" >> $env:GITHUB_OUTPUT
        echo "date_tag=$timestamp" >> $env:GITHUB_OUTPUT
      shell: pwsh
      
    # 🕐 生成精确到秒的时间戳（UTC时区） - Unix
    - name: 🕐 Generate Timestamp (Unix)
      if: matrix.os != 'windows-latest'
      id: timestamp-unix
      run: |
        timestamp=$(date -u +"%Y-%m-%d_%H-%M-%S_UTC")
        echo "timestamp=$timestamp" >> $GITHUB_OUTPUT
        echo "date_tag=$timestamp" >> $GITHUB_OUTPUT
      shell: bash
      
    # 🕐 统一时间戳输出
    - name: 🕐 Set Unified Timestamp
      id: timestamp
      run: |
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          echo "timestamp=${{ steps.timestamp-windows.outputs.timestamp }}" >> $GITHUB_OUTPUT
          echo "date_tag=${{ steps.timestamp-windows.outputs.date_tag }}" >> $GITHUB_OUTPUT
        else
          echo "timestamp=${{ steps.timestamp-unix.outputs.timestamp }}" >> $GITHUB_OUTPUT
          echo "date_tag=${{ steps.timestamp-unix.outputs.date_tag }}" >> $GITHUB_OUTPUT
        fi
      shell: bash
      
    - name: 🏗️ Build Release Binary
      env:
        # 🎯 CI专用RUSTFLAGS - 覆盖.cargo/config.toml避免CPU兼容问题
        RUSTFLAGS: >-
          ${{
            matrix.target == 'aarch64-apple-darwin' && '-C target-feature=+neon -C opt-level=3' ||
            matrix.target == 'x86_64-apple-darwin' && '-C target-feature=+sse2,+sse3,+sse4.1,+sse4.2,+avx,+avx2 -C opt-level=3' ||
            matrix.target == 'x86_64-pc-windows-msvc' && '-C target-feature=+sse2,+sse3,+sse4.1,+sse4.2,+avx,+avx2 -C opt-level=3' ||
            matrix.target == 'x86_64-unknown-linux-gnu' && '-C target-feature=+sse2,+sse3,+sse4.1,+sse4.2,+avx,+avx2 -C opt-level=3' ||
            '-C opt-level=3'
          }}
      run: |
        echo "🚀 Building release binary for ${{ matrix.platform }}-${{ matrix.arch }}..."
        echo "📋 Build environment info:"
        echo "  Target: ${{ matrix.target }}"
        echo "  OS: ${{ matrix.os }}"
        echo "  Platform: ${{ matrix.platform }}"
        echo "  Arch: ${{ matrix.arch }}"
        echo "  RUSTFLAGS: $RUSTFLAGS"
        
        echo "🔍 Verifying Rust setup:"
        rustc --version
        cargo --version
        rustup show
        
        echo "🎯 Checking if native or cross-compilation:"
        NATIVE_TARGET=$(rustc -vV | grep "host:" | cut -d' ' -f2)
        echo "  Host target: $NATIVE_TARGET"
        echo "  Build target: ${{ matrix.target }}"
        
        if [ "$NATIVE_TARGET" = "${{ matrix.target }}" ]; then
          echo "✅ Native build detected - no cross-compilation needed"
          echo "🏗️ Starting native build..."
          cargo build --release --verbose
        else
          echo "🎯 Cross-compilation detected - preparing target..."
          echo "🔍 Checking if target ${{ matrix.target }} is installed..."
          if rustup target list --installed | grep -q ${{ matrix.target }}; then
            echo "✅ Target ${{ matrix.target }} already installed"
          else
            echo "📦 Installing target ${{ matrix.target }}..."
            rustup target add ${{ matrix.target }}
            echo "✅ Target installation completed"
          fi
          
          echo "🏗️ Starting cross-compilation build..."
          cargo build --release --target ${{ matrix.target }} --verbose
        fi
        
    # 📦 准备二进制文件 - Windows
    - name: 📦 Prepare Binary with Timestamp (Windows)
      if: matrix.os == 'windows-latest'
      id: binary-windows
      run: |
        $binary_name = "dr-meter-${{ github.ref_name }}_${{ steps.timestamp.outputs.timestamp }}_${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.extension }}"
        
        # 智能路径检测 - Native vs Cross-compilation
        $native_path = "target/release/dr-meter${{ matrix.extension }}"
        $cross_path = "target/${{ matrix.target }}/release/dr-meter${{ matrix.extension }}"
        
        if (Test-Path $native_path) {
          Write-Host "✅ Using native build path: $native_path"
          $source_path = $native_path
        } elseif (Test-Path $cross_path) {
          Write-Host "✅ Using cross-compilation path: $cross_path" 
          $source_path = $cross_path
        } else {
          Write-Host "❌ Binary not found in either path!"
          Write-Host "  Native path: $native_path"
          Write-Host "  Cross path: $cross_path"
          exit 1
        }
        
        Copy-Item $source_path $binary_name
        echo "binary_name=$binary_name" >> $env:GITHUB_OUTPUT
        echo "binary_path=$binary_name" >> $env:GITHUB_OUTPUT
      shell: pwsh
      
    # 📦 准备二进制文件 - Unix
    - name: 📦 Prepare Binary with Timestamp (Unix)
      if: matrix.os != 'windows-latest'
      id: binary-unix
      run: |
        binary_name="dr-meter-${{ github.ref_name }}_${{ steps.timestamp.outputs.timestamp }}_${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.extension }}"
        
        # 智能路径检测 - Native vs Cross-compilation
        native_path="target/release/dr-meter${{ matrix.extension }}"
        cross_path="target/${{ matrix.target }}/release/dr-meter${{ matrix.extension }}"
        
        if [ -f "$native_path" ]; then
          echo "✅ Using native build path: $native_path"
          source_path="$native_path"
        elif [ -f "$cross_path" ]; then
          echo "✅ Using cross-compilation path: $cross_path"
          source_path="$cross_path"
        else
          echo "❌ Binary not found in either path!"
          echo "  Native path: $native_path"
          echo "  Cross path: $cross_path"
          exit 1
        fi
        
        cp "$source_path" "$binary_name"
        chmod +x "$binary_name"  # 确保可执行权限
        echo "binary_name=$binary_name" >> $GITHUB_OUTPUT
        echo "binary_path=$binary_name" >> $GITHUB_OUTPUT
      shell: bash
      
    # 📦 统一二进制输出
    - name: 📦 Set Unified Binary Info
      id: binary
      run: |
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          echo "binary_name=${{ steps.binary-windows.outputs.binary_name }}" >> $GITHUB_OUTPUT
          echo "binary_path=${{ steps.binary-windows.outputs.binary_path }}" >> $GITHUB_OUTPUT
        else
          echo "binary_name=${{ steps.binary-unix.outputs.binary_name }}" >> $GITHUB_OUTPUT
          echo "binary_path=${{ steps.binary-unix.outputs.binary_path }}" >> $GITHUB_OUTPUT
        fi
      shell: bash
      
    # ✅ 验证二进制文件 - Windows
    - name: ✅ Verify Binary (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        echo "🔍 Verifying built binary..."
        if (Test-Path "${{ steps.binary.outputs.binary_path }}") {
          $size = (Get-Item "${{ steps.binary.outputs.binary_path }}").Length
          Write-Host "✅ Binary verified: ${{ steps.binary.outputs.binary_name }} ($size bytes)"
        } else {
          Write-Host "❌ Binary not found!"
          exit 1
        }
      shell: pwsh
      
    # ✅ 验证二进制文件 - Unix
    - name: ✅ Verify Binary (Unix)
      if: matrix.os != 'windows-latest'
      run: |
        echo "🔍 Verifying built binary..."
        if [ -f "${{ steps.binary.outputs.binary_path }}" ]; then
          size=$(stat -c%s "${{ steps.binary.outputs.binary_path }}" 2>/dev/null || stat -f%z "${{ steps.binary.outputs.binary_path }}")
          echo "✅ Binary verified: ${{ steps.binary.outputs.binary_name }} ($size bytes)"
        else
          echo "❌ Binary not found!"
          exit 1
        fi
      shell: bash
      
    - name: 📦 Compress Binary (Unix systems)
      if: matrix.os != 'windows-latest'
      run: |
        echo "🗜️ Compressing binary to save storage..."
        gzip -9 "${{ steps.binary.outputs.binary_path }}"
        echo "compressed_path=${{ steps.binary.outputs.binary_path }}.gz" >> $GITHUB_OUTPUT
      id: compress
      
    - name: 📤 Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ steps.binary.outputs.binary_name }}
        path: ${{ matrix.os == 'windows-latest' && steps.binary.outputs.binary_path || steps.compress.outputs.compressed_path }}
        retention-days: 14  # 私有仓库优化：减少存储成本
        compression-level: ${{ matrix.os == 'windows-latest' && '9' || '0' }}  # Windows压缩，Unix已压缩
        
  # 📋 构建摘要和发布准备
  build-summary:
    name: Build Summary & Release Preparation
    needs: [changes, build]
    runs-on: ubuntu-latest
    if: always() && (needs.changes.outputs.rust == 'true' || github.event_name == 'workflow_dispatch')  # 只在有代码变更或手动触发时运行
    
    steps:
    - name: 📥 Download All Artifacts
      uses: actions/download-artifact@v4
      
    - name: 📋 Generate Build Summary
      run: |
        echo "# 🚀 MacinMeter DR Tool - Build Summary" > build_summary.md
        echo "" >> build_summary.md
        echo "## 📅 Build Information" >> build_summary.md
        echo "- **Build Date**: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> build_summary.md
        echo "- **Commit**: ${{ github.sha }}" >> build_summary.md
        echo "- **Branch**: ${{ github.ref_name }}" >> build_summary.md
        echo "- **Trigger**: ${{ github.event_name }}" >> build_summary.md
        echo "" >> build_summary.md
        echo "## 📦 Available Binaries" >> build_summary.md
        echo "" >> build_summary.md
        
        # 列出所有生成的二进制文件
        for dir in dr-meter-*; do
          if [ -d "$dir" ]; then
            binary_file=$(ls "$dir"/)
            size=$(stat -c%s "$dir/$binary_file" 2>/dev/null || echo "unknown")
            echo "- **$binary_file** - $size bytes" >> build_summary.md
          fi
        done
        
        echo "" >> build_summary.md
        echo "## 🛡️ Security & Quality" >> build_summary.md
        echo "- ✅ Code formatting check passed" >> build_summary.md
        echo "- ✅ Static analysis (Clippy) passed" >> build_summary.md  
        echo "- ✅ Security audit completed" >> build_summary.md
        echo "- ✅ Unit tests passed" >> build_summary.md
        echo "" >> build_summary.md
        echo "## 🔧 Usage Instructions" >> build_summary.md
        echo "" >> build_summary.md
        echo "1. Download the appropriate binary for your platform" >> build_summary.md
        echo "2. Make it executable (Unix systems): \`chmod +x dr-meter-*\`" >> build_summary.md
        echo "3. Run without arguments for batch mode: \`./dr-meter-*\`" >> build_summary.md
        echo "4. Or specify a file: \`./dr-meter-* audio.flac\`" >> build_summary.md
        echo "" >> build_summary.md
        echo "---" >> build_summary.md
        echo "*Generated by MacinMeter DR Tool CI/CD Pipeline*" >> build_summary.md
        
    - name: 📤 Upload Build Summary
      uses: actions/upload-artifact@v4
      with:
        name: build-summary
        path: build_summary.md
        
    - name: 📊 Display Summary
      run: cat build_summary.md
      
  # 🚀 自动发布 (仅在tag推送时)
  release:
    name: Create Release
    needs: [changes, quality-check, build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') && needs.changes.outputs.rust == 'true'  # 仅在版本tag且有代码变更时触发
    
    steps:
    - name: 📥 Download All Artifacts
      uses: actions/download-artifact@v4
      
    - name: 🏷️ Create Release
      uses: softprops/action-gh-release@v1
      with:
        draft: false
        prerelease: contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc')
        generate_release_notes: true
        files: |
          dr-meter-*/dr-meter-*
        body: |
          ## 🚀 MacinMeter DR Tool Release
          
          ### 🎯 Features
          - ✅ 符合 Measuring_DR_ENv3.md 标准的DR测量
          - 🚀 SIMD加速和多线程支持
          - 📁 批量处理模式（双击启动）
          - 🎵 支持多种音频格式 (WAV, FLAC, MP3, AAC, OGG)
          - 📊 自动生成详细分析报告
          
          ### 📦 Available Platforms
          - **Windows x64**: `dr-meter-{branch}_*_windows-x64.exe`
          - **macOS Intel**: `dr-meter-{branch}_*_macos-intel`  
          - **macOS Apple Silicon**: `dr-meter-{branch}_*_macos-arm64`
          - **Linux x64**: `dr-meter-{branch}_*_linux-x64`
          
          ### 🔧 Quick Start
          1. Download the binary for your platform
          2. Place in a folder with audio files
          3. Double-click to run (or use command line)
          4. Results saved to `DR_Analysis_Results_*.txt`
          
          **Full Changelog**: https://github.com/${{ github.repository }}/compare/${{ github.event.before }}...${{ github.sha }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}