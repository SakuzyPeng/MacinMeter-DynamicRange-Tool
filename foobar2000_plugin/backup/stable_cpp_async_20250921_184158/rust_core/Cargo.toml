[package]
name = "macinmeter-dr-core"
version = "1.0.0"
edition = "2021"
authors = ["Sakuzy Peng <sakuzy.peng@gmail.com>"]
description = "MacinMeter DR Analysis Core Engine for foobar2000 Plugin"

[lib]
name = "macinmeter_dr_core"
crate-type = ["cdylib"]

[dependencies]
# 🎯 FFI适配层依赖：仅保留必需项

# 引入主工程DR计算核心（复用所有算法）
macinmeter-dr-tool = { path = "../.." }

# C FFI支持
libc = "0.2"

[features]
default = ["simd"]
simd = []

[profile.release]
opt-level = "z"  # 优化大小
lto = true
codegen-units = 1
panic = "abort"
strip = true  # 移除调试符号